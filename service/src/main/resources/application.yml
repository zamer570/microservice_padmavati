spring:
  application:
    name:
      CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri:
            http://github.com/zamer570/microservice_config_new
          clone-on-start: true
server:
  port: 8087
